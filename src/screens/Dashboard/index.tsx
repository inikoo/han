import React, {useState} from 'react';
import {
  SafeAreaView,
  View,
  TextInput,
  Image,
  TouchableOpacity,
} from 'react-native';
import {Text, Button} from '@rneui/base';

const LoginScreen = () => {
  return (
    <View>
     <Text>dashboard</Text>
    </View>
  );
};

export default LoginScreen;
